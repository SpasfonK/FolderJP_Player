<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Thème système sans AppCompat/Material XML : l'interface est 100 % Compose. -->
    <style name="Theme.N7FolderPlayer" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/n7_night</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
